name: robotenok
description: Android application for Robotenok by CodedLife

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  cupertino_icons: ^1.0.2
  sqflite: any
  http:
  crypto: ^3.0.1
  intl: ^0.17.0
  qr_flutter: ^4.0.0
  qr_code_scanner: ^0.5.2
  camera:

  path:
  path_provider: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
     - assets/